global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: 
          - 'localhost:9090'

  
  - job_name: 'monitor_file_container'
    static_configs:
      - targets:
          - 'monitor_file_container:8001'